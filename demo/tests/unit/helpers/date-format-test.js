import {
  dateFormat
} from 'demo/helpers/date-format';

module('DateFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = dateFormat(42);
  ok(result);
});
